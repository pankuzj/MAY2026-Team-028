# Alembic configuration (placeholder).
# TODO: initialise with `alembic init app/db/migrations`; the database URL is
#       read from the environment in migrations/env.py, not hard-coded here.
[alembic]
script_location = app/db/migrations
