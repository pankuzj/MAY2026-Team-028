# syntax=docker/dockerfile:1
# SmartSweep backend image (placeholder skeleton).
# Flesh out into a multi-stage uv build once dependencies are pinned.
FROM python:3.13-slim
WORKDIR /app
# TODO: install uv; copy pyproject + lockfile and run `uv sync`; copy app/;
#       serve with `uvicorn app.main:app --host 0.0.0.0 --port 8000`.
